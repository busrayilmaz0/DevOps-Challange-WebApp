apiVersion: v1
kind: Service
metadata:
  name: {{ include "ziraat-webapp-chart.fullname" . }}
  labels:
    {{- include "ziraat-webapp-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ziraat-webapp-chart.selectorLabels" . | nindent 4 }}
